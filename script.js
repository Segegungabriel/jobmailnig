// Optional: Add client-side search enhancement (e.g., highlight matches)
document.getElementById('search')?.addEventListener('input', function() {
    // If you want to add client-side search highlighting, you can do it here
    // For now, PHP handles filtering via form submission
});